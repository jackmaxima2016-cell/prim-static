name: Deploy prim.net

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Build static site
        run: npm run build
        env:
          PUBLIC_GA4_ID: ${{ secrets.GA4_ID }}

      - name: SEO contract (bloquant)
        run: |
          python3 scripts/seo_contract.py data/audit_prim.json dist --posts data/wp/posts.json

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name prim --branch main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
